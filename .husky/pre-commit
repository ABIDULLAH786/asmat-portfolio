echo "Running pre-commit: ESLint check..."
npm run lint
if [ $? -ne 0 ]; then
  echo "Commit blocked: ESLint reported errors. Fix them or run 'npm run lint:fix'."
  exit 1
fi
echo "Pre-commit checks passed."
